import { BcAddCommasPipe } from './bc-add-commas.pipe';

describe('BcAddCommasPipe', () => {
  it('create an instance', () => {
    const pipe = new BcAddCommasPipe();
    expect(pipe).toBeTruthy();
  });
});
