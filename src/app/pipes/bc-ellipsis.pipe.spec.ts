import { BcEllipsisPipe } from './bc-ellipsis.pipe';

describe('BcEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new BcEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
